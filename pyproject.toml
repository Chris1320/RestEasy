[tool.poetry]
name = "resteasy"
version = "0.1.0"
description = "A restic frontend for automating backups."
authors = ["chris1320 <chris1320is@protonmail.com>"]
license = "MIT License"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.13"
PySide6 = "^6.6.2"
pylint = "^3.1.0"

[tool.poetry.group.dev.dependencies]
Nuitka = "^2.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
